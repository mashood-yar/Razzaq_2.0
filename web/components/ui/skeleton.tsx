/**
 * Theme-aware loading placeholders (react-loading-skeleton).
 * Must be used from client components only.
 */
"use client";

export {
  AppSkeletonTheme,
  LoadingSkeleton as Skeleton,
} from "@/components/loading/app-skeleton";
